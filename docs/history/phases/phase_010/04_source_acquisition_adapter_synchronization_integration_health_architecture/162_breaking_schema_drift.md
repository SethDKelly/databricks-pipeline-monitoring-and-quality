# ARCH-162 — Breaking Schema / API Drift

**Status:** Accepted

Removed/renamed fields, incompatible types, endpoint-version changes or contract violations become explicit integration schema-drift states.

The adapter may quarantine or narrow publication, but must not substitute guessed semantics.
