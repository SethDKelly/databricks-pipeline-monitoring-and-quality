# ARCH-176 — Network / Timeout State

**Status:** Accepted

DNS, TLS, routing, proxy, firewall and timeout failures are integration reachability states with environment/capability-instance context.

A network failure cannot be translated into source absence or source-owned negative truth.
