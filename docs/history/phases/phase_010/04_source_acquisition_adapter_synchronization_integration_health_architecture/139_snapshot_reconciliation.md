# ARCH-139 — Snapshot Reconciliation

**Status:** Accepted

Snapshot/reconciliation runs explicitly bind population, source surface, query/filter semantics and observation window so differences against prior state can be interpreted safely.

A snapshot of discoverable objects is not automatically the Monitoring Scope denominator.
