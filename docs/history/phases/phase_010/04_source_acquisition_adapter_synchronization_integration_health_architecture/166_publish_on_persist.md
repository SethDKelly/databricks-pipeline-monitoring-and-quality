# ARCH-166 — Publish Only After Durable Persistence

**Status:** Accepted

Normalized evidence becomes eligible for downstream reasoning only after its source provenance and required collection metadata are durably persisted.

Ephemeral in-memory observations are not treated as retained evidence history.
