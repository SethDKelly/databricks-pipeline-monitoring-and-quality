# ARCH-376 — Fallback Enforcement

**Status:** Accepted

Fallback enforcement is evidenced independently from fallback configuration, trigger and decision.

Where the fallback changes execution path, the chosen path receives its own Group 05 execution/provenance evidence.
