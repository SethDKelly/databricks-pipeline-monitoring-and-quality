# ARCH-390 — Databricks Conditional Gate Adapter

**Status:** Accepted

Databricks `If/else` or `Run if` logic may realize a DMTZ Gate only when exact DMTZ criterion, opportunity, branches and execution outcomes are explicitly mapped and evidenced.

Native condition configuration alone is not automatically a DMTZ Gate.
