# ARCH-253 — Statement / Query Encounter Join

**Status:** Accepted

Where source systems expose stable statement/query IDs, they are used to join execution/query records to Lineage/consumer evidence.

Join availability is source/mode specific and absence preserves uncertainty.