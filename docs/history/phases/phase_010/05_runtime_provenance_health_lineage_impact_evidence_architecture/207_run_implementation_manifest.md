# ARCH-207 — Run-Specific Implementation Manifest

**Status:** Accepted

A run implementation manifest composes the exact evidenced code, task/job configuration, parameters, runtime/compute, libraries, environment and external configuration references material to reproducibility.

Missing facets remain missing rather than inherited from current state.