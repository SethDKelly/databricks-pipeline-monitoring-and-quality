# ARCH-221 — Multi-Input Consumption Manifest

**Status:** Accepted

A multi-input execution retains the exact evidenced set of consumed input states and their run/task/query context.

One known input version never fills unknown sibling inputs or proves current-cycle alignment.