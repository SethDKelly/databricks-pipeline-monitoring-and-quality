# ARCH-213 — Environment / Feature Binding

**Status:** Accepted

Environment, workspace, region, feature/capability profile and material runtime flags are bound to run reconstruction when they can alter behavior.

Public/default capability state is never substituted for the realized deployment profile.