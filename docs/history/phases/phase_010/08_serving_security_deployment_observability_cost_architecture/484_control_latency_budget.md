# ARCH-484 — Control Latency Budget

**Status:** Accepted

SC-06 latency is opportunity-specific and bounded by criterion/evidence retrieval, deterministic evaluation, authorization, decision delivery and enforcement deadlines plus the decision applicability horizon.

A decision that arrives outside its governed opportunity/TTL is stale, not a successful low-availability control.