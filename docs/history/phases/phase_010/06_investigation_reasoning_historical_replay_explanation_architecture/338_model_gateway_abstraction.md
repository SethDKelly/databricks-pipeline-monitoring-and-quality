# ARCH-338 — Model Gateway Abstraction

**Status:** Accepted

Model invocation uses a provider-neutral gateway interface; Databricks Unity AI Gateway/model services are a preferred conditional realization when verified, while external providers remain policy/deployment choices.

This contract is subordinate to accepted Phase 002–009 semantics and ARCH-001–ARCH-337.