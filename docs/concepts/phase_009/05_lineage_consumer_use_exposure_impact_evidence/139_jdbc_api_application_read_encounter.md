# INTG-139 — JDBC/API/Application Read Encounter

**Status:** Accepted — Phase 009 Group 05

A captured JDBC/ODBC/SQL/API query plus lineage read can establish a platform-level read by its principal/client context even when no Databricks entity metadata exists.

Application fetch/processing/user display remain separate boundaries unless application telemetry closes them.
