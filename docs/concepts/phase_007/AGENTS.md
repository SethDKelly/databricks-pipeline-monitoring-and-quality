# Phase 007 Agent Handoff

Applies to work under `docs/concepts/phase_007/` and complements the repository root `AGENTS.md` without replacing accepted cross-phase rules.

## Current status

- Phase 006 is complete with HLTH-001–HLTH-066 final.
- Phase 007 grouping is accepted.
- Phase 007 uses `OPS-###` refinement contracts.
- No OPS contracts are accepted yet.
- **Group 01 — Lineage Relationship Taxonomy, Historical Topology & Operational Relevance is next and has not started.**
- Accepted concept count remains 24; SYN-001–SYN-035, REF-001–REF-030, AUTH-001–AUTH-053 and HLTH-001–HLTH-066 remain unchanged.

## Accepted grouping

1. Lineage Relationship Taxonomy, Historical Topology & Operational Relevance.
2. Change Intent, Deployment Realization & Realized Change.
3. Prospective Blast Radius & Change-Aware Review.
4. Execution Reconstruction, Dependency Sequence & Version Use.
5. Investigation Lifecycle, First-Deviation Localization & Causal Handoff.
6. Impact, Consumer Encounter, Exposure & Consequence.
7. Propagation Safeguard Scope, Enforcement, Release & Recovery.
8. Execution Gate, Fallback/Override & Control-Induced Operational Effects.
9. Historical Operational Replay & Consolidation / Exit Review.

## Permanent Phase 007 boundaries

Preserve:

- Lineage ≠ causality;
- Lineage relationship ≠ relevance to every field/population/version/consumer;
- Lineage relation ≠ metric/status propagation;
- planned topology ≠ active topology ≠ historical topology;
- Change Intent ≠ Deployment ≠ realized Change;
- deployment attempt ≠ activation ≠ intended outcome;
- prospective blast radius ≠ actual Impact;
- intended schedule/dependency ≠ actual execution sequence;
- execution occurrence ≠ specific consumed-version proof unless evidenced;
- missing telemetry ≠ no event;
- first-deviation localization ≠ root cause;
- Investigation ≠ Causal Claim truth;
- candidate/reachable ≠ exposed ≠ effect ≠ consequence ≠ causal attribution;
- `not exposed` requires adequate opportunity/path coverage;
- Safeguard proposal/configuration/active state ≠ enforcement ≠ prevented exposure;
- safeguard release ≠ healthy/fresh output;
- Phase 006 health suitability ≠ readiness ≠ gate decision ≠ enforcement ≠ execution;
- gate HOLD ≠ execution failure;
- gate ADMIT ≠ execution occurrence;
- override ≠ prerequisite ready;
- configured fallback ≠ actual fallback application;
- Execution Gate ≠ Propagation Safeguard;
- control-induced delay/staleness/non-delivery remains observable/assessable and is not automatically defect/cause;
- historical operational replay uses event/effective time plus knowledge cut and remains non-rewriting.

## Group 01 entry contract

Group 01 should define the minimum operational Lineage taxonomy and historical-topology evidence rules needed by later change/Impact/control reasoning.

Do not add a new Concept merely because multiple relationship classes exist. Extend/reopen the accepted Lineage concept only if a genuine independent truth owner is proven necessary.

Do not infer source authority from availability, record count, recency or tool identity. Phase 005 Assertion Authority remains applicable to disputed topology assertions.

## Architecture boundary

Do not select graph database, graph query language, Lineage ingestion source, event store, scheduler/orchestrator, safeguard/quarantine mechanism, gate implementation, queue/event bus, cache/streaming topology, RCA algorithm, persistence schema, source integration or latency/timeout SLA in Phase 007 functional refinement.

Do not begin Group 01 or a later group without explicit user direction.
