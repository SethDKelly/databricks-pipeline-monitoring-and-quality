# ARCH-002 — Capability Instance Identity

**Status:** Accepted

A usable integration capability is bound to a concrete capability instance: vendor/product, deployment model, cloud/hosting context, region/Geo, account/tenant, workspace/metastore/repository scope, edition/plan/version, and exact feature or source surface.

A vendor name alone is never a sufficient capability identity.
