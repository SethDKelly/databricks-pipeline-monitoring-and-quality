# ARCH-110 — Authorization Decision Record

**Status:** Accepted

Material authorization evaluations can persist an immutable decision record containing policy revision, principal identity, membership inputs, subject, action, context, decision state, decision time and evaluation provenance.

The record proves the evaluation occurred, not that an action was later enforced or executed.

Retention is service-/audit-class dependent rather than indefinite by default.