# ARCH-082 — Entity Identity Tenant Scope

**Status:** Accepted

Every canonical entity identity is bound to an organization/tenant boundary and an explicit entity class.

Cross-tenant equality is never inferred from matching names, paths, IDs, or hashes.

Federated or shared entities require explicit governed mapping evidence.