# ARCH-130 — Policy Evaluation Input Manifest

**Status:** Accepted

Material scope/authority/authorization/disclosure evaluations can retain a manifest of policy revision, identity bindings, memberships, classifications/policy context, request context and other inputs used.

This supports replay and audit without claiming every input is independently authoritative.

Unavailable input remains explicit rather than silently defaulted.