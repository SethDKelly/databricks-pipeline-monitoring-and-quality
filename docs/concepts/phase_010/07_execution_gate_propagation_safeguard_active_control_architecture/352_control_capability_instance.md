# ARCH-352 — Control Capability Instance

**Status:** Accepted

Every Gate or Safeguard enforcement mechanism binds a Group 01 capability instance, deployment scope, version/plan/feature state, permissions, reachability and known limitations.

Vendor documentation alone does not establish usable enforcement in a target enterprise deployment.
