# ARCH-054 — Opaque / Large Payload Object Plane

**Status:** Accepted

Opaque or large retained artifacts that are unsuitable for structured rows SHALL use a separately governed cloud-object payload plane referenced by durable evidence manifests.

Unity Catalog volumes are preferred only where verified; the logical payload plane cannot depend on that feature universally.
