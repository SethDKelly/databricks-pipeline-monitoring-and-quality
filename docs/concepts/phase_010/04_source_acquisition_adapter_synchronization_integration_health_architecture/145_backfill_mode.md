# ARCH-145 — Historical Backfill Mode

**Status:** Accepted

Backfill runs use explicit historical windows and are labeled separately from routine current acquisition.

Backfilled evidence preserves original source/event coordinates and later collection availability; it never rewrites earlier as-known state.
