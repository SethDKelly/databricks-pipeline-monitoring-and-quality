# ARCH-173 — Rate-Limit Handling

**Status:** Accepted

HTTP 429, quota exhaustion and vendor-specific throttle signals become explicit quota integration-health states with limit/reset/retry metadata where exposed.

Rate limiting narrows current collection capability; it never changes domain truth or evidence authority.
