# ARCH-156 — Raw-Before-Normalize Preservation

**Status:** Accepted

Where payload class and minimization policy permit, the adapter durably captures source-owned bytes/fields or an integrity-preserving representation before semantic normalization.

Normalization never replaces provenance for what the source actually returned.
