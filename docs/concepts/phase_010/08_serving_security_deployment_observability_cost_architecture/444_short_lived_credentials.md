# ARCH-444 — Short-Lived Credential Preference

**Status:** Accepted

Short-lived federated/OAuth/OIDC credentials are preferred over long-lived static credentials when the verified target capability supports the required workload.

Static credentials, when unavoidable, require explicit rotation, storage and exposure controls rather than becoming architectural defaults.