# ARCH-468 — Configuration as Versioned State

**Status:** Accepted

Runtime topology, adapter, service-class, security and cost-control configuration is version-controlled/revision-addressed where material, while secret values remain externalized.

Historical behavior binds the effective configuration revision rather than current defaults.