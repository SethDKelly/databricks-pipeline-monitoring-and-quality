# ARCH-496 — Optional Model / Search / Control Cost

**Status:** Accepted

Model tokens/invocations, vector/search indexing/query work and active-control adapter/service usage are separately attributable where measurable.

Repeated model calls are not justified as corroboration and cost pressure may disable optional assistance without weakening deterministic truth/control rules.