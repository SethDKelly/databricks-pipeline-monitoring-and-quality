# ARCH-481 — Acquisition Latency Budget

**Status:** Accepted

Near-current/periodic acquisition latency is decomposed into source publication availability, scheduling/poll delay, request/retry, normalization, canonical persistence and projection availability rather than one opaque freshness number.

Only applicable components constrain a particular proposition.