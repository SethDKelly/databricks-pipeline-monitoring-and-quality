# ARCH-493 — Acquisition Cost

**Status:** Accepted

Acquisition observes request/query volume, source-side query/compute where exposed, transfer, normalization and reconciliation processing costs/usage drivers sufficient for bounded optimization and forecasting.

Cost reduction cannot silently remove required collection coverage.