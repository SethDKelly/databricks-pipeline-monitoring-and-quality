# ARCH-461 — MVP Logical Topology

**Status:** Accepted

The MVP is Databricks-centered for canonical Delta evidence/policy state and batch/reconciliation processing, with a thin stateless API/service façade and independently deployable edge/control adapters where interactive or cross-system pre-start enforcement requires them.

This is a logical topology; exact hosting products remain deployment-profile decisions.