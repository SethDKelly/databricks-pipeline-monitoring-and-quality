# ARCH-443 — Workload Identity

**Status:** Accepted

Acquisition, serving, reasoning, archive and active-control workloads use distinct least-privilege service/workload identities where deployment capabilities permit.

One omnipotent shared credential is rejected as the default runtime topology.