# ARCH-485 — Capacity Model

**Status:** Accepted

Capacity planning models acquisition volume, canonical write rate, retained data, concurrent queries, graph/replay work, model calls, control opportunities and archive restores by tenant/service class and growth assumptions.

One average request-rate estimate is insufficient for bursty control/reconciliation workloads.