# ARCH-470 — Deployment Rollback

**Status:** Accepted

Runtime rollback restores a known application/configuration revision without rolling canonical evidence/policy history backward or erasing actions that occurred while the newer revision was active.

Rollback is deployment recovery, not historical rewrite.