# ARCH-460 — Security Degradation

**Status:** Accepted

Identity, policy, secret, network or audit dependency degradation is surfaced explicitly and mapped to service/control-specific governed behavior; it cannot silently become permissive access or fabricated denial/domain absence.

SC-06 behavior follows the exact Gate/Safeguard degraded-dependency policy.