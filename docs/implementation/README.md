# DMTZ Implementation Program

**Status:** NEXT / READY — IMPLEMENTATION 001-A NOT STARTED

**ADF status mirror: COMPLETE ADF-A–ADF-H; ADF-EX-17 DEFERRED VERIFICATION; FOUNDATION EXIT ACCEPTED.**

**CKR status mirror: COMPLETE CKR-A–CKR-K; CKR EXIT ACCEPTED.**

**DPTN exit: ACCEPTED — Implementation 001-A NEXT / READY / NOT STARTED.**

ADF, CKR and DPTN exits are accepted. Product implementation has not started.

Current repository-native discovery starts at `docs/index.md`; exact semantic ownership remains the CKR ownership inventory; known IDs use the deterministic stable-ID resolver. Top-level `knowledge/` is generated OKF compatibility only. `docs/history/` remains provenance only.

Current work:

- ADF — EXIT ACCEPTED;
- CKR-A–K — COMPLETE / ACCEPTED;
- CKR EXIT — ACCEPTED;
- DPTN-A–G — COMPLETE / ACCEPTED;
- DPTN EXIT — ACCEPTED;
- **Implementation 001-A — NEXT / READY / NOT STARTED**.

DPTN exit retired migration/compatibility paths and preserved its completed record under `docs/history/retrofits/dptn/`. Current operational routing uses first-class semantic owners, permanent discovery surfaces and the implementation program directly.

No product source, schemas, tests, migrations, deployment configuration, source adapters or implementation traceability claims were created by DPTN. Releasing 001-A from the topology blocker is a planning/status transition only.

**Implementation 001-A may begin only after a subsequent explicit human-selected implementation task.**

Accepted ranges remain SYN-001–035, REF-001–030, AUTH-001–053, HLTH-001–066, OPS-001–123, EXPL-001–160, INTG-001–270 and ARCH-001–500.
