# Implementation Agent / Developer Instructions

**ADF status mirror: COMPLETE ADF-A–ADF-H; ADF-EX-17 DEFERRED VERIFICATION; FOUNDATION EXIT ACCEPTED.**

**CKR status mirror: COMPLETE CKR-A–CKR-K; CKR EXIT ACCEPTED.**

**DPTN status mirror: COMPLETE DPTN-A–DPTN-D; NEXT DPTN-E; IMPLEMENTATION 001-A BLOCKED ON DPTN EXIT.**

ADF and CKR are complete/accepted. DPTN-A–D are complete/accepted and DPTN-E is NEXT / READY / NOT STARTED. **Implementation 001-A is BLOCKED / NOT STARTED** until DPTN-G exit acceptance and a later explicit human-selected implementation task.

Use `docs/canonical_knowledge_retrofit/canonical_ownership_inventory.json` as current semantic-owner routing. Current owners live in first-class `docs/<family>/` paths. For a known stable ID, `scripts/agentic/resolve_stable_id.py <ID>` returns the current locator selected by the ledger; `--history` is provenance-only.

DPTN-D decomposed completed CKR evidence into `docs/history/retrofits/ckr/` and completed ADF program evidence into `docs/history/foundations/adf/`; neither history tree may satisfy current semantic or operational-policy authority. DPTN routing, manifests, validators and conformance results are not product semantics or implementation evidence.
