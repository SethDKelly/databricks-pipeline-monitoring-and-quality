# Implementation Agent / Developer Instructions

**ADF status mirror: COMPLETE ADF-A–ADF-H; ADF-EX-17 DEFERRED VERIFICATION; FOUNDATION EXIT ACCEPTED.**

**CKR status mirror: COMPLETE CKR-A–CKR-K; CKR EXIT ACCEPTED.**

**DPTN status mirror: COMPLETE DPTN-A–DPTN-B; IN EXECUTION DPTN-C; IMPLEMENTATION 001-A BLOCKED ON DPTN EXIT.**

ADF and CKR are complete/accepted. DPTN-A–B are complete/accepted and DPTN-C is IN EXECUTION. **Implementation 001-A is BLOCKED / NOT STARTED** until DPTN-G exit acceptance and a later explicit human-selected implementation task.

Use `docs/canonical_knowledge_retrofit/canonical_ownership_inventory.json` as current semantic-owner routing throughout DPTN-C. Current owner paths may change only through the synchronized MOVE-007–MOVE-014 promotion. For a known stable ID, `scripts/agentic/resolve_stable_id.py <ID>` returns the current locator selected by the ledger; `--history` is provenance-only. `docs/history/` and legacy redirect paths must never satisfy current semantic resolution as owners.

DPTN routing, manifests, validators, compatibility redirects and conformance results are not product semantics or implementation evidence. DPTN-C authorizes no product source, schema, adapter, runtime or deployment implementation.
