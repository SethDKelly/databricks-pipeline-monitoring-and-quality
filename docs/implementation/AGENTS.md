# Implementation Agent / Developer Instructions

**ADF status mirror: COMPLETE ADF-A–ADF-H; ADF-EX-17 DEFERRED VERIFICATION; FOUNDATION EXIT ACCEPTED; IMPLEMENTATION 001-A NEXT.**

**CKR status mirror: COMPLETE CKR-A–CKR-J; NEXT CKR-K; IMPLEMENTATION 001-A BLOCKED ON CKR EXIT.**

CKR-A–J are complete/accepted. CKR-K is next/ready but unstarted. Product implementation remains blocked until CKR-K.

Use the ownership inventory and canonical owners for semantic authority. For a known stable ID, `scripts/agentic/resolve_stable_id.py <ID>` returns the canonical locator; `--history` is provenance-only. `knowledge/` remains optional routing when location is unknown.

CKR-J routing helpers, manifests and conformance results are not product semantics or implementation evidence.

Do not create product source/schemas/tests/deployment configuration as CKR work. CKR-J completion does not authorize CKR-K or implementation work.
