# Canonical Compatibility Index

**Authority state:** ROUTING / COMPATIBILITY ONLY — SUBSTANTIVE OWNERS PROMOTED BY DPTN-C

This `docs/canonical/` directory is **not a substantive semantic owner**. DPTN-E merged its authored orientation role into the single discovery root **`docs/index.md`** ([open it](../index.md)); DPTN-F rebound current agent/rule/reference consumers away from this compatibility namespace.

Current semantic authority is selected by `docs/canonical_knowledge_retrofit/canonical_ownership_inventory.json` at first-class `docs/concepts/`, `docs/architecture/`, `docs/authority/`, `docs/contracts/`, `docs/experience/`, `docs/invariants/`, `docs/policies/`, and `docs/reference/` paths.

The family entries beneath this directory may remain temporarily as compatibility redirects until DPTN-G. Redirect presence never establishes semantic authority, never becomes a stable-ID locator, and never competes with the ownership inventory. Current operational routing must not depend on these redirects.

For normal repository-native discovery use `docs/index.md`. For a known stable ID, use `python3 scripts/agentic/resolve_stable_id.py <ID>`; use `--history` only for explicit provenance/history work. Generated `knowledge/index.md` remains OKF v0.2 compatibility only.

DPTN-F changes routing references only. It does not change the 24-concept catalog, SYN-001–035, REF-001–030, AUTH-001–053, HLTH-001–066, OPS-001–123, EXPL-001–160, INTG-001–270, ARCH-001–500, the frozen reference architecture, or implementation state.

## Remaining normalization

- **DPTN-E — COMPLETE / ACCEPTED:** orientation converged into `docs/index.md`; top-level `knowledge/` is generated compatibility output.
- **DPTN-F — COMPLETE / ACCEPTED:** current stable-reference, agent/rule/link and drift-analysis consumers rebound to normalized paths.
- **DPTN-G — NEXT / READY:** final compatibility-path/scaffolding retirement, whole-program conservation audit and exit review.

Implementation 001-A remains blocked until DPTN-G exit acceptance.
