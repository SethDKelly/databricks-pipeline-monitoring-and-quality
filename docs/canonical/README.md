# Canonical Compatibility Index

**Authority state:** ROUTING / COMPATIBILITY ONLY — SUBSTANTIVE OWNERS PROMOTED BY DPTN-C

This `docs/canonical/` directory is **not a substantive semantic owner**. DPTN-E merged its authored orientation role into the single discovery root at [`../index.md`](../index.md).

Current semantic authority is selected by `docs/canonical_knowledge_retrofit/canonical_ownership_inventory.json` at first-class `docs/concepts/`, `docs/architecture/`, `docs/authority/`, `docs/contracts/`, `docs/experience/`, `docs/invariants/`, `docs/policies/`, and `docs/reference/` paths.

The family entries beneath this directory may remain as temporary compatibility redirects until DPTN-F/G. Redirect presence never establishes semantic authority, never becomes a stable-ID locator, and never competes with the ownership inventory.

For normal discovery use [`../index.md`](../index.md). For a known stable ID, use `python3 scripts/agentic/resolve_stable_id.py <ID>`; use `--history` only for explicit provenance/history work.

DPTN-E changes routing/discovery topology only. It does not change the 24-concept catalog, SYN-001–035, REF-001–030, AUTH-001–053, HLTH-001–066, OPS-001–123, EXPL-001–160, INTG-001–270, ARCH-001–500, the frozen reference architecture, or implementation state.

## Remaining normalization

- **DPTN-E — COMPLETE / ACCEPTED:** orientation converged into `docs/index.md`; top-level `knowledge/` is generated compatibility output.
- **DPTN-F — NEXT / READY:** broad stable-reference, agent/OKF/link rebinding.
- **DPTN-G — PLANNED:** final legacy-path retirement and exit conservation review.

Implementation 001-A remains blocked until DPTN-G exit acceptance.
