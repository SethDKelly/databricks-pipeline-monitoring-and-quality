---
description: Accepted Phase 005 disclosure/Explanation/audience guardrails after Phase 005 exit.
globs:
alwaysApply: true
---

# Phase 005 disclosure governance — accepted

Status: **AUTH-044–AUTH-053 accepted. Phase 005 Group 07 consolidation/exit is accepted; Phase 005 is complete. Phase 006 is next and has not started.**

Preserve:

- disclosure target = requester/audience + information/detail class + subject/context + purpose + temporal perspective + delivery scope;
- audience labels do not grant authorization;
- view ≠ publish/export/forward/client disclosure;
- result visibility ≠ basis/detail/source/actor/authority visibility;
- hidden/restricted basis ≠ absent basis;
- safe abstraction/redaction/opacity must preserve proposition meaning, material uncertainty, and exact status;
- opaque existence is permitted only when existence itself may be disclosed;
- aggregation/redaction ≠ automatic declassification;
- evaluate material mosaic/differencing/repeated-query leakage, not only single-field permission;
- technical/business/executive/audit/client views are one truth at different authorized detail;
- high-consequence communication review/approval ≠ evidence sufficiency/causal confirmation/health/compliance/enforcement;
- `supported` ≠ `confirmed`; `reachable` ≠ `exposed`; `not exposed to V` ≠ `fresh`; `hold decided` ≠ `hold enforced`; `safeguard active` ≠ `prevented exposure`; `released` ≠ `healthy`; `waived` ≠ clean pass;
- confirmer/approver/operator/authority-holder/delegation/break-glass/service-principal metadata can be independently sensitive;
- Annotation disclosed without exact author identity remains human-provided context where material;
- retained historical Explanation ≠ reconstructed as-known-then Explanation ≠ retrospective Explanation;
- current requester authorization governs present historical disclosure;
- unknown/conflicting/unavailable/unsafe-to-project disclosure state never becomes permission;
- withholding under unresolved disclosure does not fabricate explicit deny or false absence.

Phase 005 exit confirms these disclosure rules compose with Assertion Authority, normative governance, Capability Authorization, high-consequence controls, and Phase 004 evidence without another concept or AUTH contract.

Do not:

- create a second business/executive truth model;
- invent a new Disclosure/Report/Audience concept;
- let communication review alter causal/health/control truth;
- treat authorized disclosure as declassification of the underlying evidence;
- select UI, LLM, report-template, redaction, differential-privacy, query-throttling, or policy-engine architecture during Phase 006 health semantics work;
- weaken these constraints when Phase 008 later designs Explanation UX.
