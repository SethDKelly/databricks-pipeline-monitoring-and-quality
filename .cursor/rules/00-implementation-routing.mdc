---
description: Route DMTZ work to live DPTN/implementation status and current semantic ownership. Use when project status, authority, workflow or reference location is unclear.
globs:
alwaysApply: false
---

# Agentic / implementation routing

**ADF status mirror: COMPLETE ADF-A–ADF-H; ADF-EX-17 DEFERRED VERIFICATION; FOUNDATION EXIT ACCEPTED.**

**CKR status mirror: COMPLETE CKR-A–CKR-K; CKR EXIT ACCEPTED.**

**DPTN status mirror: COMPLETE DPTN-A–DPTN-F; NEXT DPTN-G; IMPLEMENTATION 001-A BLOCKED ON DPTN EXIT.**

ADF and CKR are complete/accepted. DPTN-A–F are complete/accepted. DPTN-G is NEXT / READY / NOT STARTED. Implementation 001-A is BLOCKED / NOT STARTED until DPTN-G exit acceptance and a later explicit human-selected implementation task.

Primary shared authority remains root `AGENTS.md`, `docs/agentic_development_foundation/authority_scope_policy.md`, the CKR ownership inventory, accepted ADF scope/security mechanics and live DPTN topology authority.

For unknown-location repository-native discovery start at `docs/index.md`. Generic OKF-compatible tools may still enter through generated `knowledge/index.md`; that projection is routing only and must not be treated as semantic authority. For a known stable ID, use `python3 scripts/agentic/resolve_stable_id.py <ID>` for the **current** locator selected by the ownership inventory; use `--history` only for provenance/history.

DPTN-F completed MOVE-019 broad stable-reference/agent/rule/link rebinding. Current routing must not depend on historical Phase paths, DPTN-D history fallback, or `docs/canonical/<family>` compatibility redirects. DPTN-G owns final legacy-path/scaffolding retirement and exit review.

Do not begin DPTN-G or product implementation without explicit human selection.
