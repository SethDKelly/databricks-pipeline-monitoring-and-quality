---
description: Route DMTZ work to current implementation status and semantic ownership. Use when project status, authority, workflow or reference location is unclear.
globs:
alwaysApply: false
---

# Agentic / implementation routing

**ADF status mirror: COMPLETE ADF-A–ADF-H; ADF-EX-17 DEFERRED VERIFICATION; FOUNDATION EXIT ACCEPTED.**

**CKR status mirror: COMPLETE CKR-A–CKR-K; CKR EXIT ACCEPTED.**

**DPTN exit: ACCEPTED — Implementation 001-A NEXT / READY / NOT STARTED.**

ADF, CKR and DPTN are complete/accepted. Implementation 001-A is NEXT / READY / NOT STARTED and still requires a later explicit human-selected implementation task.

Primary shared authority remains root `AGENTS.md`, `docs/agentic_development_foundation/authority_scope_policy.md`, the CKR ownership inventory and accepted ADF scope/security mechanics.

For unknown-location repository-native discovery start at `docs/index.md`. Generic OKF-compatible tools may enter through generated `knowledge/index.md`; that projection is routing only and must not be treated as semantic authority. For a known stable ID, use `python3 scripts/agentic/resolve_stable_id.py <ID>` for the current canonical locator selected by the ownership inventory; use `--history` only for provenance/history.

Current routing must use first-class semantic-owner paths and current implementation packages. Completed DPTN evidence is provenance under `docs/history/retrofits/dptn/` and is not a live routing layer.

Do not begin product implementation solely because 001-A is ready. Start it only when the human explicitly selects that task.
