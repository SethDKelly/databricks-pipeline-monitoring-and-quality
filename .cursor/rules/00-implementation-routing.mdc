---
description: Route DMTZ work to live DPTN/implementation status and current semantic ownership. Use when project status, authority, workflow or reference location is unclear.
globs:
alwaysApply: false
---

# Agentic / implementation routing

**ADF status mirror: COMPLETE ADF-A–ADF-H; ADF-EX-17 DEFERRED VERIFICATION; FOUNDATION EXIT ACCEPTED.**

**CKR status mirror: COMPLETE CKR-A–CKR-K; CKR EXIT ACCEPTED.**

**DPTN status mirror: COMPLETE DPTN-A–DPTN-E; NEXT DPTN-F; IMPLEMENTATION 001-A BLOCKED ON DPTN EXIT.**

ADF and CKR are complete/accepted. DPTN-A–E are complete/accepted. DPTN-F is NEXT / READY / NOT STARTED. Implementation 001-A is BLOCKED / NOT STARTED until DPTN-G exit acceptance and a later explicit human-selected implementation task.

Primary shared authority remains root `AGENTS.md`, `docs/agentic_development_foundation/authority_scope_policy.md`, the CKR ownership inventory, accepted ADF scope/security mechanics and live DPTN topology authority.

For unknown-location discovery start at `docs/index.md`. Top-level `knowledge/` is generated OKF compatibility only. For a known stable ID, use `python3 scripts/agentic/resolve_stable_id.py <ID>` for the **current** locator selected by the canonical ownership inventory; use `--history` only for provenance/history.

DPTN-E converged the authored discovery root and preserved the former hand-maintained OKF plane in history. Do not infer authority from generated OKF, history, redirects, path vacancy, move maps, recency, search order, vendor skill, tool memory, source availability or path presence.

Do not begin DPTN-F/G or product implementation without explicit human selection.
