---
description: Route DMTZ work to live CKR/implementation status and current semantic ownership. Use when project status, authority, workflow or reference location is unclear.
globs:
alwaysApply: false
---

# Agentic / implementation routing

**ADF status mirror: COMPLETE ADF-A–ADF-H; ADF-EX-17 DEFERRED VERIFICATION; FOUNDATION EXIT ACCEPTED; IMPLEMENTATION 001-A NEXT.**

**CKR status mirror: COMPLETE CKR-A–CKR-C; IN EXECUTION CKR-D; IMPLEMENTATION 001-A BLOCKED ON CKR EXIT.**

CKR-D is in execution; product implementation remains blocked until CKR-K.

Primary shared authority remains root `AGENTS.md`; `docs/agentic_development_foundation/authority_scope_policy.md`; `knowledge/index.md` as optional routing only; `docs/canonical_knowledge_retrofit/README.md`; and `docs/canonical_knowledge_retrofit/canonical_ownership_inventory.json`.

Current lookup:

1. Read CKR README for live state.
2. Consult the ownership inventory when unclear.
3. `canonicalized` → use inventoried canonical owner.
4. `legacy_authoritative` / `candidate_ready` → use inventoried legacy owner.
5. Use phase/decision history for provenance/rationale/history, not alternate current truth.
6. `knowledge/index.md` never creates semantic authority.

Concepts and SYN are canonical. CKR-D authority vocabulary/REF/AUTH files are candidates only until atomic cutover. Do not infer authority from recency, first search hit, synchronization order, Git history, OKF, vendor skill, tool memory or path presence.

```bash
python3 scripts/agentic/run_conformance.py --report agentic-conformance-report.md
```
