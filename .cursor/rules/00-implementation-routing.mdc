---
description: Route DMTZ work to live CKR/implementation status and current semantic ownership. Use when project status, authority, workflow or reference location is unclear.
globs:
alwaysApply: false
---

# Agentic / implementation routing

**ADF status mirror: COMPLETE ADF-A–ADF-H; ADF-EX-17 DEFERRED VERIFICATION; FOUNDATION EXIT ACCEPTED; IMPLEMENTATION 001-A NEXT.**

**CKR status mirror: COMPLETE CKR-A; IN EXECUTION CKR-B; IMPLEMENTATION 001-A BLOCKED ON CKR EXIT.**

CKR-B has completed its nine-record foundation/glossary cutover and awaits closure validation. Product implementation remains blocked until CKR-K.

## Current semantic questions

1. Read `docs/canonical_knowledge_retrofit/README.md` for live CKR state.
2. Consult `canonical_ownership_inventory.json` when ownership is unclear.
3. `canonicalized` → use the inventoried `docs/canonical/` target.
4. `legacy_authoritative` / `candidate_ready` → use the inventoried legacy owner.
5. Use phase/decision history for provenance/rationale/history/change review, not alternate current truth.
6. `knowledge/index.md` remains optional routing only.

CKR-B product definition, actors/stakeholders, terminology, Concept Design method, AP-01–32, SP-01–15, lifecycles, MVP boundary and shared glossary now resolve to `docs/canonical/`. Their Phase-001/legacy glossary sources are provenance.

All 24 concepts and stable-ID families remain with later CKR groups.

Primary controls:

- root `AGENTS.md`;
- `docs/agentic_development_foundation/authority_scope_policy.md` — A1–A4;
- CKR `authority_model.md` / `migration_contract.md`;
- `docs/implementation/README.md` — implementation block;
- `.agents/skills/<name>/SKILL.md` — workflows/overlays;
- `scripts/agentic/resolve_stable_id.py` — occurrence discovery;
- `scripts/agentic/run_conformance.py` — validation.

Do not infer authority from phase recency, first search hit, Git history, OKF summary, vendor skill, tool memory or path presence alone. Genuine semantic conflicts require A4 change control.

ADF-EX-17 / `ADF-G-XT01` remains deferred runtime verification. `DBX-SKILL-RUN-01` remains future 001-A work. Reviewed Databricks vendor skills remain operational guidance only.

```bash
python3 scripts/agentic/run_conformance.py --report agentic-conformance-report.md
```

PASS is repository agentic/documentation-authority conformance, not DMTZ domain health, provider-runtime proof, Databricks capability or production readiness.
