---
description: Route DMTZ work to live implementation/DPTN status and current semantic ownership. Use when project status, authority, workflow or reference location is unclear.
globs:
alwaysApply: false
---

# Agentic / implementation routing

**ADF status mirror: COMPLETE ADF-A–ADF-H; ADF-EX-17 DEFERRED VERIFICATION; FOUNDATION EXIT ACCEPTED; IMPLEMENTATION 001-A NEXT.**

**CKR status mirror: COMPLETE CKR-A–CKR-K; CKR EXIT ACCEPTED; IMPLEMENTATION 001-A NEXT.**

DPTN status mirror: COMPLETE DPTN-A; NEXT DPTN-B; IMPLEMENTATION 001-A BLOCKED ON DPTN EXIT.

CKR-A–K and DPTN-A are complete/accepted. DPTN-B is NEXT / READY / NOT STARTED. Implementation 001-A remains blocked until DPTN exit.

Primary shared authority remains root `AGENTS.md`, `docs/agentic_development_foundation/authority_scope_policy.md`, the CKR ownership inventory, and accepted ADF scope/security mechanics. All accepted semantic families through ARCH remain canonicalized under the existing CKR paths until later validated DPTN cutovers; Phase 001–010 sources remain provenance.

For a known stable ID, use `python3 scripts/agentic/resolve_stable_id.py <ID>` to get the deterministic current canonical locator. Use `--history` only for explicit provenance/history. For an unknown semantic location, use one bounded route from `knowledge/index.md`, then the current canonical owner.

DPTN-A acceptance does not authorize DPTN-B relocation. Future DPTN paths in the accepted move map are not current authority until later validated cutovers. Do not move existing documentation, rebind the resolver, change stable-ID meaning, or create product implementation without the applicable explicitly selected task.

Do not infer authority from DPTN planning metadata, OKF, resolver output alone, recency, search order, Git history, vendor skill, tool memory, source availability or path presence.
