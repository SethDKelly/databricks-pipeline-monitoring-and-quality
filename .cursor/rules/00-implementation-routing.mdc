---
description: Route DMTZ work to live CKR/implementation status and current semantic ownership. Use when project status, authority, workflow or reference location is unclear.
globs:
alwaysApply: false
---

# Agentic / implementation routing

**ADF status mirror: COMPLETE ADF-A–ADF-H; ADF-EX-17 DEFERRED VERIFICATION; FOUNDATION EXIT ACCEPTED; IMPLEMENTATION 001-A NEXT.**

**CKR status mirror: COMPLETE CKR-A; IN EXECUTION CKR-B; IMPLEMENTATION 001-A BLOCKED ON CKR EXIT.**

CKR-B is in execution. Its nine foundation/glossary targets are `candidate_ready`; the inventoried legacy sources remain current authority until atomic cutover. Product implementation remains blocked until CKR-K.

## Current semantic questions

1. Use `docs/canonical_knowledge_retrofit/README.md` for CKR state.
2. If ownership is unclear, read `docs/canonical_knowledge_retrofit/canonical_ownership_inventory.json`.
3. `legacy_authoritative` / `candidate_ready` → legacy owner remains current.
4. `canonicalized` → inventoried `docs/canonical/` target is current.
5. Use phase/decision history only for provenance/rationale/history/change review after cutover.
6. `knowledge/index.md` is optional routing only.

Primary controls:

- `AGENTS.md` — shared constitution;
- `docs/agentic_development_foundation/authority_scope_policy.md` — A1–A4;
- `docs/canonical_knowledge_retrofit/authority_model.md` — canonical vs history;
- `docs/canonical_knowledge_retrofit/migration_contract.md` — atomic cutover/no dual authority;
- `docs/canonical_knowledge_retrofit/ckr_b_semantic_conservation_matrix.md` — CKR-B review evidence;
- `docs/implementation/README.md` — implementation block;
- `.agents/skills/<name>/SKILL.md` — workflows/overlays;
- `scripts/agentic/resolve_stable_id.py` — occurrence discovery;
- `scripts/agentic/run_conformance.py` — conformance.

Do not infer authority from phase recency, first search hit, Git history, OKF summary, vendor skill, tool memory or mere presence under `docs/canonical/`.

CKR-B may consolidate accepted current foundation meaning but may not absorb CKR-C–I domains, create product code, or invent semantics. Genuine conflicts require A4 change control. Stable-ID meaning survives path migration.

ADF-EX-17 / `ADF-G-XT01` remains deferred runtime verification. `DBX-SKILL-RUN-01` remains a future 001-A obligation after CKR unlock. Reviewed Databricks vendor skills remain operational guidance only.

```bash
python3 scripts/agentic/run_conformance.py --report agentic-conformance-report.md
```

PASS is repository agentic/documentation-authority conformance, not DMTZ domain health, provider-runtime proof, Databricks capability or production readiness.
